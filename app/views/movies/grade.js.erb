
$("#message").html("<h3 id='vote-computed'>Vote Computed</h2>");
	$('#grade-input').fadeOut(2000, function() {
	  $('#btn-vote').fadeOut(500, function() {
	  	$(this).remove();
	  	});
	  });

setTimeout(function(){
	$("#message").fadeOut(2000, function(){
		$(this).remove();
	});
});
